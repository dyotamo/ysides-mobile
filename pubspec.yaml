name: sides
description: Choose your side

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http:
  percent_indicator: ^2.1.4
  device_id: ^0.2.0
  fluttertoast: ^4.0.1
  share: ^0.6.4+3
  grouped_list: ^3.1.0
  shimmer: ^1.1.1
  flutter_icons: ^1.1.0
  cupertino_icons: ^0.1.3
  shake: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
